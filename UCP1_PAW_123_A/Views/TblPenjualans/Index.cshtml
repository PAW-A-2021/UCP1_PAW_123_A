@model IEnumerable<UCP1_PAW_123_A.Models.TblPenjualan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TglKwitansi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPelangganNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TglKwitansi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPelangganNavigation.IdPelanggan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NoKwitansi">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NoKwitansi">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NoKwitansi">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
